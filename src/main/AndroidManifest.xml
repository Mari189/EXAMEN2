<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="xyz.mariaestherguaman.esteticakira">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.EsteticaKira">
        <activity android:name=".MainActivity3contacto"></activity>
        <activity
            android:name=".ContactoActivity3"
            android:label="@string/title_activity_contacto3"
            android:theme="@style/Theme.EsteticaKira.NoActionBar" />
        <activity android:name=".CursoActivity" />
        <activity android:name=".MainActivity2" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>